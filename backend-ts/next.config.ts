import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "standalone" is disabled when using the custom server.js
  // with Socket.io. Re-enable if you switch back to `next start`.
};

export default nextConfig;
